# Adapter Codex

Delivers an explicit user request to an existing Codex task using `codex queue`. System process
access stays here; other layers pass only a task ID and message.

See [canonical architecture](../../docs/canonical/architecture.md) and
[local API](../../docs/canonical/local-api.md).
